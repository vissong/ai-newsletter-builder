# Build artifacts — do not commit raw collection data
data/raw/

# In particular, never commit gmail raw bodies — they can contain
# unsubscribe tokens and per-recipient tracking links unique to your inbox.
data/raw/**/gmail-*.md

# macOS / editor scratch
.DS_Store
*.swp
*~

# Local config overrides (if you add any)
config/*.local.yaml
